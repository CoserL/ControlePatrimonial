@model IEnumerable<ControlePatrimonios.categoria>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Adicionar", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.Label("Categoria")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.descCategoria)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.idCategoria }) |
            @Html.ActionLink("Detalhes", "Details", new { id=item.idCategoria }) |
            @Html.ActionLink("Deletar", "Delete", new { id=item.idCategoria })
        </td>
    </tr>
}

</table>
