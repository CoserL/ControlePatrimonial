@model IEnumerable<ControlePatrimonios.ben>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Adicionar", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.Label("Descrição")
        </th>
        <th>
            @Html.Label("Categoria")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.descBem)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.categoria.descCategoria)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.idBem, idCat = item.idCategoria}) |
            @Html.ActionLink("Detalhes", "Details", new { id = item.idBem, idCat = item.idCategoria }) |
            @Html.ActionLink("Deletar", "Delete", new { id = item.idBem, idCat = item.idCategoria })
        </td>
    </tr>
}

</table>
